<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pch.Ex_1210">

    <!-- theme 수정시 activity의 AppCompatActivity를 Activity로 수정해주어야 한다 -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light">
        <activity android:name=".ButtonEventActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WeightActivity2">

        </activity>
        <activity android:name=".WeightActivity"></activity>
        <activity android:name=".ListViewActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>